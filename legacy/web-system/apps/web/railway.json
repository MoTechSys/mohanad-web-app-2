{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @grocery/shared build && pnpm --filter @grocery/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @grocery/web exec vite preview --host 0.0.0.0 --port ${PORT:-4173}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
