# سجل الإصدارات — دفتر البقالة

<div dir="rtl">

> الترقيم: `MAJOR.MINOR.PATCH+build` · كل إصدار يمر بـ `flutter analyze` (0 مشاكل) و`flutter test` كاملًا قبل الدفع.

## [2.2.1+5] — تدقيق شامل: إصلاح وردية شبح + اتساق التقارير + تنظيف + تقليص الحجم

> نتيجة مراجعة معمّقة للمستودع (كود، اختبارات، أندرويد، توثيق). **دُمج في `main`** (PR #1).

### الحجم (APK arm64: 14.0MB → **13.3MB** قياسي / **10.2MB** lite)
- أيقونات اللانشر كانت 192×192 في كل الكثافات الخمس (500KB) + ملفات `_foreground` غير مُشار إليها → أُعيد توليدها بحجم كل كثافة (48→192) بصيغة RGB: **164KB**.
- R8: قواعد keep دقيقة لـ ML Kit والجسر الأصلي، 5 تمريرات تحسين، `repackageclasses`، إزالة `Intrinsics` و`Log.v/d/i`.
- استبعاد `*.properties` و`kotlin/**` و`DebugProbesKt.bin` و`META-INF/*` من الحزمة.
- `localeFilters = ar` فقط (كانت ar+en).
- بناء `--obfuscate --split-debug-info` (رموز التصحيح خارج الـ APK).
- **نسخة lite** جديدة: `useUnbundled=true` يُخرج نموذج الباركود (3MB) ليحمّله Play Services عند أول مسح. موثَّقة في `gradle.properties` وREADME.
- الإصدارات: `dist/` (مُتجاهَل في git) + GitHub Releases.

### إصلاحات
- **🐞 وردية شبح بعد الاستعادة** — `LedgerDb.wipeAll()` لم يكن يمسح `cashSessions` من الذاكرة، فعند استعادة نسخة احتياطية أثناء وردية مفتوحة كانت الوردية تعود للقرص كـ«شبح» وتمنع فتح وردية جديدة. أُصلح بقائمة موحّدة `_allCollections` + `assert` يضمن أن كل صندوق له مجموعة مطابقة، حتى لا يتكرر النسيان مع أي صندوق مستقبلي.
- **مسح الباركود من منتج** — كان المسح يحفظ نصًا فارغًا بدل `null`؛ الآن `Product.copyWith` يستخدم sentinel (مثل `expiryDate`) فيميّز «بلا تغيير» عن «امسح». `updateProduct(barcode: '')` يمسح فعلًا ويحرّر الباركود لمنتج آخر.
- **توحيد تطبيع الباركود** عند الإنشاء والتعديل (`normalizeBarcode`: أرقام عربية → ASCII، إزالة الفراغات) — ما يُدخل من الكيبورد العربي يطابق ما يقرأه الماسح.

### اتساق التقارير
- **سندات القبض من جهات خارجية** (بدون عميل) صارت تدخل «إجمالي الداخل» في تقرير الفترة ولوحة التحكم — حقل جديد `PeriodSummary.otherReceipts` — لتتطابق مع تقرير Z للوردية. لا تُحسب إيرادًا (لا تمس الربح). تظهر في شاشة التقارير (عند وجودها) وفي PDF وExcel كـ«مقبوضات أخرى (سندات)».

### تجربة المستخدم
- **تحذير عند الاستعادة أثناء وردية مفتوحة** في كلا المسارين (ملف / نص ملصوق): يعرض رقم الوردية والعامل وينصح بإغلاقها أولًا.

### بناء وإعدادات
- `gradle.properties`: `-Xmx4G` بدل 8G (كان يسبب OOM على أجهزة CI بـ8GB) + تفعيل `caching` و`parallel`.
- `build.gradle.kts`: إزالة تعليقات Flutter الافتراضية (TODO) وتوثيق أماكن تزامن معرّف الحزمة.

### توثيق
- README: تصحيح الإصدار (build 5)، إضافة قسم م6 (كان مفقودًا)، Android **7.0+ (API 24)** بدل 5.0، النسخ `.glbak` المضغوطة، عدّاد الاختبارات.
- USER_GUIDE: قسم النسخ الاحتياطي بنمط واتساب + الإشعارات + خيارات المرونة (إخفاء الماسح / خط أكبر).
- DEVELOPMENT: فخّ «صندوق جديد ⇒ أضفه في 3 أماكن» + ملاحظة sentinel.
- CHANGELOG: إصلاح موضع العنوان (كان بعد مدخل 2.2.0).

### اختبارات
- **157/157 اختبار ناجح** (+5): regression للوردية الشبح، `wipeAll` يمسح كل المجموعات، اتساق `otherReceipts` بين تقرير الفترة وZ، دورة حياة الباركود (إبقاء/مسح/تعيين + الفهرس)، `copyWith` sentinel.

## [2.2.0+4] — م6: تجربة مستخدم مبسّطة + نسخ احتياطي بنمط واتساب

### تصميم وواجهة (لأصحاب المحلات الذين لا يقرؤون جيدًا)
- **لون جديد**: أزرق مالي عميق (#1D6FE0) بدل الأخضر — أنسب لتطبيق محاسبة وأكثر ثقة
- **أيقونة تطبيق جديدة** زرقاء متوافقة مع الهوية الجديدة
- **«المزيد» صار شبكة أيقونات كبيرة ملوّنة** بدل القائمة النصية — الأيقونة واللون هما الدليل
- تحليل معماري: أُبقيت التبويبات السفلية (أوضح من القائمة الجانبية المخفية لغير القارئين)

### إعدادات المرونة
- **إخفاء ماسح الباركود**: للمحلات بلا باركود — يخفي الكاميرا من الكاشير والمنتجات وبلاطة الكاشير من المزيد
- **خط أكبر**: يكبّر كل نصوص التطبيق 15% لكبار السن وضعاف القراءة
- توافق خلفي كامل: النسخ القديمة تُقرأ بالقيم الافتراضية

### نسخ احتياطي بنمط واتساب (م6a)
- النسخ اليومية صارت **مضغوطة gzip بامتداد `.glbak`** بدل JSON مكشوف
- تُحفظ في **مجلد مرئي** يظهر في مدير الملفات مثل واتساب:
  `Android/media/com.groceryledger.accounts/دفتر البقالة/Databases/`
  (لا يحتاج أي أذونات تخزين على أندرويد 11+)
- زر **«نسخ احتياطي الآن»** لنسخة فورية بأحدث البيانات
- **نافذة استعادة فورية**: تفتح مباشرة بقائمة كل النسخ (التاريخ + الحجم) والمستخدم يختار
- توافق خلفي: نسخ JSON القديمة (م5) تُكتشف وتُستعاد بلا مشاكل (كشف gzip بالبايتات السحرية)
- شاشة نسخ احتياطي جديدة كليًا بنمط واتساب + خيارات JSON المتقدمة تحت قسم قابل للطي

### إشعارات محلية (م6b)
- **إشعار أندرويد في شريط الإشعارات** عند وجود منتجات منتهية/قريبة الانتهاء (ليس داخل التطبيق فقط)
- قناة إشعارات `grocery_alerts` + طلب إذن POST_NOTIFICATIONS تلقائي على أندرويد 13+
- جسر نيتيف جديد: `NativeBridge.showNotification`

### تقارير (م6c)
- **تقرير الأرباح الشهري PDF**: إيرادات، تكلفة، مجمل وصافي الربح (دقيق+تقديري)، أعلى الأصناف ربحًا، المصروفات بالفئات، الإيراد اليومي
- **زر معاينة لكل تقارير PDF**: شاشة معاينة كاملة (PdfPreview) مع طباعة ومشاركة وحفظ مدمجة
- كل تقرير الآن: **معاينة + مشاركة + طباعة + حفظ**

### حفظ في مجلد عام مرئي (م6e)
- «حفظ في الجهاز» يحفظ الآن في **`Download/دفتر البقالة/`** الظاهر في مدير الملفات
- عبر MediaStore على أندرويد 10+ (بدون أذونات) مع مسار قديم لأندرويد 9
- جسر نيتيف جديد: `NativeBridge.saveToDownloads`

### اختبارات
- 152/152 اختبار ناجح (7 اختبارات نسخ محدثة للصيغة المضغوطة + 5 اختبارات إعدادات جديدة)

## [2.1.0] — م2 مكتمل + تنظيف الحجم (أندرويد فقط)

### تنظيف جذري للحجم (تدقيق صفري)
- **أندرويد فقط**: إزالة مجلد `web/` ومنع كل المنصات الأخرى (ios/linux/macos/windows) عبر `.gitignore`.
- إزالة `legacy/web-system` (23MB نظام ويب قديم) و`dist/` من التتبع.
- تنظيف تاريخ git من الملفات الثنائية (APKs وصور قديمة): **67MB ← 15MB**.
- حجم المشروع الكلي: **276MB ← 42MB** (منها 25MB نسخ APK جاهزة قابلة للحذف).
- إزالة الحزمة غير المستخدمة `barcode_widget`.
- ملاحظة: حجم APK النهائي للجهاز ~7–8MB فقط (split-per-abi + minify + shrinkResources + ضغط المكتبات الأصلية + خط عربي واحد).

### م2d: شاشة سندات القبض والصرف
- قائمة السندات مع فلترة (الكل/قبض/صرف) وتمييز الملغى.
- نموذج إنشاء: قبض/صرف، عميل أو مورّد مسجّل (مع عرض دينه/مستحقاته) أو طرف خارجي باسم يدوي، المبلغ، طريقة الدفع (نقدًا/حوالة/شبكة)، التاريخ، «وذلك مقابل».
- إجراءات لكل سند: طباعة A5 رسمي، إيصال حراري 80mm، مشاركة PDF، إلغاء بسبب إجباري (يعكس الأثر).
- زر «طباعة» فوري بعد إنشاء السند.
- مدخل جديد في قائمة «المزيد»: «سندات القبض والصرف».

### م2e: رسائل SMS مباشرة (SmsManager)
- `NativeBridge.sendSms` + تنفيذ Kotlin عبر `SmsManager` (تقسيم تلقائي للرسائل الطويلة).
- صلاحية `SEND_SMS` في Manifest + طلب الصلاحية وقت التشغيل مع إعادة الإرسال تلقائيًا بعد الموافقة.
- `SmsService` بقوالب عربية:
  - بيع آجل: المبلغ + إجمالي الدين (يُرسل تلقائيًا بعد حفظ البيع الآجل — أفضل جهد لا يعطّل الحفظ).
  - سند قبض: المدفوع + المتبقي (أو «تم سداد كامل المبلغ») — زر في إجراءات السند.
  - تذكير بالدين: من قائمة ملف العميل (يظهر فقط عند وجود دين ورقم هاتف).
- واتساب وغيره يبقى بالمشاركة عبر النظام (حسب المتطلب: «الرسائل ضروري تكون مباشرة، وواتساب يقدر يشاركه»).

### اختبارات
- +7 اختبارات جديدة (شاشة السندات: قائمة/إنشاء/فلترة + قوالب SMS والإرسال والتخطي الآمن) — **128/128 ناجحة**.

### م3: ورديات الصندوق + تقرير Z (مكتمل)
- نموذج `CashSession` (رقم Z-متسلسل لا يُعاد استخدامه، عامل، رصيد افتتاحي، إغلاق بجرد فعلي) + `ZReport` مشتق بالكامل من الدفاتر خلال نافذة الوردية.
- `CashSessionService`:
  - فتح وردية (اسم عامل + افتتاحي) — **وردية واحدة مفتوحة كحد أقصى** (خطأ sessionOpen عند محاولة ثانية).
  - إغلاق بجرد فعلي — «المتوقع» يُحسب لحظة الإغلاق **ويُجمَّد داخل السجل** فلا يتغير التقرير لاحقًا؛ منع الإغلاق المزدوج (sessionClosed).
  - تقرير Z: مبيعات نقدية (عدد+مبلغ)، مبيعات آجلة (معلومة — لا تدخل الدرج)، سدادات عملاء (تشمل سندات القبض لعملاء)، مقبوضات خارجية، دخل يومي، مصروفات، **النقد المتوقع = الافتتاحي + الداخل − الخارج**، والفرق (زيادة/عجز/مطابق).
- شاشة «ورديات الصندوق»: فتح/إغلاق مع عرض المتوقع الحي والفرق قبل التأكيد، قائمة الورديات مع وسم الفرق، عرض تقرير Z تفصيلي + **طباعة إيصال Z حراري 80mm** + مشاركة PDF.
- صندوق تخزين `cash_sessions` + دمج كامل في النسخ الاحتياطي (تصدير/استيراد JSON) — النسخ القديمة تُستورد بأمان.
- سجل تدقيق لكل فتح/إغلاق.
- +6 اختبارات (ترقيم/منع مزدوج، تحقق مدخلات، حساب تقرير Z كامل، تجميد المتوقع والفرق، نافذة الوردية، الاستمرارية والنسخ الاحتياطي).

### م4: تواريخ الصلاحية والتنبيهات (مكتمل)
> من الملاحظات الصوتية: «تواريخ انتهاء الصلاحية مع تنبيه قبل الانتهاء»

- حقل `expiryDate` اختياري في المنتج (null = منتج بلا صلاحية كالمواد غير الغذائية):
  - `isExpired` — هل انتهى فعلًا؟
  - `daysToExpiry` — الأيام المتبقية (سالب = منتهٍ).
  - `copyWith` بنمط sentinel: عدم تمرير التاريخ يُبقيه، تمريره null يمسحه صراحةً.
- خدمة المخزون: `createProduct(expiryDate:)` و`updateProduct(expiryDate:, clearExpiry:)` — المسح صريح فقط، التعديلات الأخرى لا تلمس التاريخ.
- تقارير جديدة:
  - `expiringSoon({days: 30})` — المنتجات النشطة التي قاربت أو تجاوزت الانتهاء، مرتبة من الأقرب انتهاءً.
  - `expiredProducts()` — المنتهية فعلًا.
- **تنبيه في لوحة التحكم**: «N منتج قارب انتهاء الصلاحية» (برتقالي) أو «N منتج منتهي الصلاحية» (أحمر) — ينقل لشاشة المنتجات.
- شاشة المنتجات:
  - وسم على بطاقة المنتج: «منتهي» بالأحمر أو «ينتهي خلال N يوم» بالبرتقالي (نافذة 30 يومًا).
  - نموذج المنتج: منتقي تاريخ صلاحية اختياري (حتى 10 سنوات مستقبلًا) مع زر مسح واضح.
- **توافق خلفي كامل**: النسخ الاحتياطية القديمة (بدون المفتاح) تُستورد بأمان — التاريخ يبقى null.
- +6 اختبارات (round-trip، تعديل/مسح، sentinel، منتهٍ فعلًا، نافذة التقرير والترتيب، استيراد نسخة قديمة + تصدير/استيراد JSON) — **140/140 ناجحة**.

### م5: النسخ الاحتياطي اليومي التلقائي (مكتمل)
> من الملاحظات الصوتية: «نسخة احتياطية تلقائية كل يوم، ويحتفظ بآخر 7 نسخ»

- خدمة `BackupService`: عند أول تشغيل كل يوم تُكتب نسخة JSON كاملة في `backups/backup-YYYY-MM-DD.json` داخل مستندات التطبيق.
- **مرة واحدة يوميًا (idempotent)** — التشغيل المتكرر في نفس اليوم لا يعيد الكتابة.
- **الاحتفاظ بآخر 7 نسخ فقط** — الأقدم يُحذف تلقائيًا.
- آمنة تمامًا: أي فشل يُبتلع ولا يعطل إقلاع التطبيق أبدًا (fire-and-forget في main).
- شاشة «النسخ الاحتياطي»: قائمة النسخ التلقائية مع **نسخ للحافظة** و**استعادة** (بتأكيد مدمر) لكل نسخة.
- +3 اختبارات (idempotency، الاحتفاظ بـ7 وحذف الأقدم، استعادة كاملة من النسخة التلقائية) — **143/143 ناجحة**.

### تحسين ختامي: مشاركة النسخة التلقائية
- زر **مشاركة** لكل نسخة يومية في شاشة النسخ الاحتياطي — إرسال ملف JSON مباشرة لواتساب/درايف/إيميل (`ShareService.shareFile`).
- +1 اختبار (مشاركة عبر spy مع التحقق من المحتوى) — **144/144 ناجحة**.

> اكتملت بذلك كل المراحل م1–م5 من متطلبات أصحاب البقالات. التوثيق الكامل في GitHub.

## [2.1.0] — سجل مراحل التطوير (م1، م2)

### 2.1.0 — مرحلة التطوير م2 (جزء المحرك): سندات القبض والصرف

> من الملاحظات المكتوبة لأصحاب البقالات: «سند قبض وسند صرف رسمي»

#### سندات القبض والصرف (Voucher)
- نموذج `Voucher` جديد (صندوق `vouchers`): إضافة فقط، إلغاء بسبب — لا حذف
- ترقيم تسلسلي بشري لا يتكرر أبدًا: `RV-0001` للقبض، `PV-0001` للصرف (حتى بعد الإلغاء)
- **سند قبض من عميل** ⇒ سطر «سداد» في دفتر العميل (ينقص دينه) مرتبط بالسند + فك حالة المهلة تلقائيًا عند تصفير الرصيد
- **سند صرف لمورد** ⇒ دفعة تسدد ديننا (سطر دفتر + مصروف `supplierPayment` مرتبطان)
- **سند لجهة خارجية** (اسم حر): قبض مستقل / صرف يسجَّل مصروفًا عامًا
- **الإلغاء يعكس كل الآثار**: سطر الدفتر + المصروف معًا، والأرصدة تعود بدقة تامة
- تعدادات جديدة (append-only): `VoucherType`، `VoucherMethod` (نقدًا/حوالة/شبكة)، `RefType.voucher/voucherCancel`
- النسخ الاحتياطي JSON يشمل السندات (النسخ القديمة بدونها تُستورد بسلام)

#### طباعة السند الرسمية (PDF)
- `voucherA5`: سند A5 أفقي بتصميم مستندي معتمد — ترويسة المحل والشعار، رقم تسلسلي، المبلغ رقمًا **وكتابةً (تفقيط)**، «استلمنا من/صرفنا إلى»، «وذلك مقابل»، طريقة الدفع، 3 خانات توقيع، وعلامة «ملغى» بالأحمر
- `voucherReceipt80`: نسخة حرارية 80mm للطابعات الصغيرة
- مكتبة تفقيط عربية `tafqit.dart`: «فقط ثلاثمائة وخمسون ريالًا لا غير» بقواعد تمييز صحيحة (آحاد/عشرات/مئات/آلاف/ملايين)

#### اختبارات
- 19 اختبارًا جديدًا: ترقيم، أرصدة قبل/بعد، إلغاء وعكس آثار، أطراف خارجية، ثبات بعد إعادة التحميل، تصدير/استيراد JSON، تفقيط — **الإجمالي 121 اختبارًا ناجحًا**

#### المتبقي من م2 (الجلسة القادمة)
- شاشة السندات (قائمة + إنشاء + طباعة + إلغاء) وربطها بقائمة «المزيد» وبملف العميل/المورد
- SMS مباشر: `NativeBridge.sendSms` + `SmsManager` + صلاحية `SEND_SMS` + قوالب (بيع آجل: المبلغ+الإجمالي / سداد: المدفوع+المتبقي) + مشاركة واتساب

### 2.1.0 — مرحلة التطوير م1: الوحدات المتعددة وضوابط البيع

> مبنية على متطلبات أصحاب البقالات (المقطع الصوتي جعبوس1 + الملاحظات المكتوبة)

#### نظام الوحدات المتعددة (كرتون، جوتة، قرطاس، كيس، دبة، قارورة، سطل…)
- `PackUnit`: وحدة عبوة لكل منتج (اسم + عامل تحويل + سعر بيع/شراء اختياري للعبوة)
- **المخزون يُخزَّن دائمًا بالوحدة الأساسية فقط** — التحويل يتم عند إدخال المستند (`baseQty = qty × unitFactor`) فلا انحراف في الأرصدة أبدًا
- `DocLine` يحمل `unitName`/`unitFactor` (السجلات القديمة تُقرأ كوحدة أساسية تلقائيًا — توافق خلفي كامل)
- نموذج المنتج: محرر وحدات عبوة كامل (إضافة/تعديل/حذف) + قائمة وحدات يمنية شائعة `kCommonUnits`
- محرر الأصناف: منتقي وحدة لكل سطر مع تعبئة السعر تلقائيًا (سعر العبوة إن وُجد، وإلا سعر الأساس × العدد)
- عرض الكمية بالوحدة (`qtyLabel`) في: بطاقات الأصناف، تفاصيل الفاتورة، PDF (فاتورة A4 وإيصال 80mm)
- الكاشير: **ضغطة مطولة** على الصنف السريع تفتح منتقي الوحدة (حبة/كرتون…) بسعر كل وحدة — سطر العبوة مستقل عن سطر الحبة في السلة
- تصدير Excel: عمود «الوحدة» في ورقة أصناف المبيعات

#### ضوابط البيع (قابلة للتخصيص من الإعدادات)
- **منع البيع بأكثر من المخزون** («اشتريت 5 كرتون ما يقبلش تبيع 10») — تجميع كل أسطر المنتج بالوحدة الأساسية قبل الفحص؛ عند إيقافه يتحول لتنبيه مع تأكيد
- **تنبيه البيع بأقل من التكلفة** مع حوار تأكيد «على مسؤوليتك»
- **تحديث أسعار المنتجات من فاتورة الشراء** تلقائيًا (يشمل اشتقاق تكلفة الأساس من سعر العبوة)
- نمط أعلام موافقة متراكمة في الكاشير وفاتورة البيع: تجاوز حد الائتمان + تجاوز المخزون + البيع بخسارة تُعرض تِباعًا بحوارات تأكيد
- 3 مفاتيح جديدة في الإعدادات: `blockOversell` (مفعّل)، `warnBelowCost` (مفعّل)، `updatePricesFromPurchase` (مفعّل)

#### بحث عربي متسامح مع الأخطاء («لو تغير حرف حرفين يقبل»)
- `ArabicText`: توحيد الهمزات (أ/إ/آ→ا)، ة→ه، ى→ي… + مسافة Levenshtein محدودة (خطأ واحد للكلمات القصيرة، خطآن للأطول)
- ترتيب النتائج: تطابق البادئة 100، احتواء 80، تقريبي 60–40 + تعزيز الباركود

#### الأساس التقني
- `Qty.times()` لضرب الكميات (2 كرتون × 24 = 48 حبة) بتقريب صحيح بدون كسور عائمة
- أكواد أخطاء جديدة: `BELOW_COST`, `SESSION_OPEN`, `SESSION_CLOSED` (تمهيدًا لصناديق الورديات)

#### اختبارات
- +20 اختبارًا (`test/domain/units_and_guards_test.dart`): الوحدات، منع تجاوز المخزون، البيع بخسارة، تحديث الأسعار، البحث المتسامح — المجموع **98**

---

## 2.0.0 (build 2) — 2026-09-03

### هوية المحل والطباعة
- شاشة **هوية المحل والطباعة**: شعار (كاميرا/معرض، ≤ 512px، Base64 محلي)، اسم المحل، المالك، الهاتف، العنوان، البيان العلوي، البيان السفلي، معاينة حيّة
- `AppSettings`: حقول `address`, `receiptHeader`, `receiptFooter`, `logoBase64` + `clearLogo`

### PDF (خط Noto Naskh Arabic مضمّن)
- فاتورة بيع A4، إيصال 80mm (Code128 لرقم الفاتورة)
- كشف حساب عميل/مورد (كامل أو لفترة)
- تقرير الفترة: KPIs، تدفق نقدي، رسم مبيعات يومية، أعلى الأصناف، مصروفات بالفئة، المدينون، السجلات
- **تقرير شهري** مع اختيار أي شهر من آخر 24 شهرًا + رقاقة «الشهر الماضي»
- تقرير المخزون (A4 أفقي)
- **ملصقات باركود**: 3 أحجام (4/3/2 أعمدة)، نسخ متعددة، EAN-13/EAN-8/UPC-A أو Code128؛ أكواد داخلية `GL…` للأصناف بلا باركود

### Excel
- مصنّف الفترة (ملخص، مبيعات، مشتريات، مصروفات)، ملف العملاء، المخزون — RTL، أرقام حقيقية

### مشاركة / طباعة / حفظ
- `ShareService` فوق `share_plus` و`printing` و`path_provider`؛ واجهة `ExportButton` / `showExportSheet` موحّدة في: التقارير، تفاصيل الفاتورة، العميل، المورد، الأصناف، الكاشير

### إصلاحات
- زر «الكاشير» العائم يظهر في الرئيسية فقط (كان يتداخل مع أزرار الشاشات الداخلية)
- `EmptyState` قابلة للتمرير (كانت تفيض على الشاشات الصغيرة)

### اختبارات
- +11 اختبار تصدير (`test/features/export_test.dart`) — المجموع **78**

---

## 2.0.0 (build 1) — 2026-09-03

- **الكاشير (POS)** بمسح الكاميرا (`mobile_scanner`) وقارئ HID، صوت واهتزاز عبر `NativeBridge`
- مسح باركود في محرّر البنود (مشتريات/استلام) وفي نموذج الصنف
- ثيم **فاتح / داكن / نظام** محفوظ
- شاشة **حول التطبيق والمطور** (معين العباسي · alabbasi.uk · +967770941666)
- إعدادات البناء: `com.groceryledger.accounts`، أيقونة مخصّصة، توقيع Release، R8 + تقليص، ضغط المكتبات الأصلية، split-per-abi، obfuscate

## 1.0.0 — 2026-09-03

- إعادة كتابة كاملة من نظام الويب إلى Flutter (أوفلاين، مستخدم واحد)
- محرك محاسبي: `Money`/`Qty` صحيحة، دفاتر append-only، أرصدة مشتقة، إلغاء بدل حذف
- شاشات: الرئيسية، العملاء، الموردون، المبيعات، المشتريات، المصروفات، الدخل اليومي، المخزون، الأصناف، التقارير، الإعدادات، النسخ الاحتياطي، PIN، سجل التدقيق
- 43 اختبار وحدة للمحرك + اختبارات واجهة
- نقل نظام الويب القديم إلى `legacy/web-system/`


</div>
